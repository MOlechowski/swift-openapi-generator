// Generated by swift-openapi-generator, do not modify.
@_spi(Generated) import OpenAPIRuntime
#raw("#if os(Linux)")
@preconcurrency import struct Foundation.URL
@preconcurrency import struct Foundation.Data
@preconcurrency import struct Foundation.Date
#raw("#else")
import struct Foundation.URL
import struct Foundation.Data
import struct Foundation.Date
#raw("#endif")
/// A type that performs HTTP operations defined by the OpenAPI document.
#(accessModifier) protocol APIProtocol: Sendable {
    #for(operation in operations):
    /// #(operation.summary)
    #if(operation.description):
    ///
    /// #(operation.description)
    #endif
    ///
    /// - Remark: HTTP `#(operation.method) #(operation.path)`.
    /// - Remark: Generated from `#/paths/#(operation.path)/#(lowercased(operation.method))(#(operation.operationId))`.
    func #(operation.operationId)(_ input: Operations.#(uppercaseFirst(operation.operationId)).Input) async throws -> Operations.#(uppercaseFirst(operation.operationId)).Output
    #endfor
}

/// Convenience overloads for operation inputs.
extension APIProtocol {
    // Convenience methods would be generated here based on operation parameters
}

/// Server URLs defined in the OpenAPI document.
#(accessModifier) enum Servers {
    // Server definitions would go here
}

/// Types generated from the components section of the OpenAPI document.
#(accessModifier) enum Components {
    /// Types generated from the `#/components/schemas` section of the OpenAPI document.
    #(accessModifier) enum Schemas {
        #for(schema in schemas):
        #if(schema.type == "object"):
        /// - Remark: Generated from `#/components/schemas/#(schema.name)`.
        #(accessModifier) struct #(schema.name): Codable, Hashable, Sendable {
            #for(property in schema.properties):
            #if(property.description):
            /// #(property.description)
            ///
            #endif
            /// - Remark: Generated from `#/components/schemas/#(schema.name)/#(property.name)`.
            #(accessModifier) var #(property.name): #(property.type)?
            #endfor
            
            /// Creates a new `#(schema.name)`.
            ///
            /// - Parameters:
            #for(property in schema.properties):
            ///   - #(property.name): #(property.description)
            #endfor
            #(accessModifier) init(
                #for(property in schema.properties):
                #(property.name): #(property.type)? = nil,
                #endfor
            ) {
                #for(property in schema.properties):
                self.#(property.name) = #(property.name)
                #endfor
            }
            
            public enum CodingKeys: String, CodingKey {
                #for(property in schema.properties):
                case #(property.name)
                #endfor
            }
        }
        #else:
        /// - Remark: Generated from `#/components/schemas/#(schema.name)`.
        #(accessModifier) typealias #(schema.name) = #(schema.swiftType)
        #endif
        #endfor
    }
    /// Types generated from the `#/components/parameters` section of the OpenAPI document.
    #(accessModifier) enum Parameters {}
    /// Types generated from the `#/components/requestBodies` section of the OpenAPI document.
    #(accessModifier) enum RequestBodies {}
    /// Types generated from the `#/components/responses` section of the OpenAPI document.
    #(accessModifier) enum Responses {}
    /// Types generated from the `#/components/headers` section of the OpenAPI document.
    #(accessModifier) enum Headers {}
}

/// API operations, with input and output types, generated from `#/paths` in the OpenAPI document.
#(accessModifier) enum Operations {
    #for(operation in operations):
    /// #(operation.summary)
    #if(operation.description):
    ///
    /// #(operation.description)
    #endif
    ///
    /// - Remark: HTTP `#(operation.method) #(operation.path)`.
    /// - Remark: Generated from `#/paths/#(operation.path)/#(lowercased(operation.method))(#(operation.operationId))`.
    #(accessModifier) enum #(uppercaseFirst(operation.operationId)) {
        #(accessModifier) static let id: Swift.String = "#(operation.operationId)"
        #(accessModifier) struct Input: Sendable, Hashable {
            #if(operation.hasQuery):
            /// - Remark: Generated from `#/paths#(operation.path)/#(lowercased(operation.method))/query`.
            #(accessModifier) struct Query: Sendable, Hashable {
                // Query parameters would be generated here
                #(accessModifier) init() {}
            }
            #(accessModifier) var query: Operations.#(uppercaseFirst(operation.operationId)).Input.Query
            #endif
            #if(operation.hasHeaders):
            /// - Remark: Generated from `#/paths#(operation.path)/#(lowercased(operation.method))/header`.
            #(accessModifier) struct Headers: Sendable, Hashable {
                #(accessModifier) var accept: [OpenAPIRuntime.AcceptHeaderContentType<Operations.#(uppercaseFirst(operation.operationId)).AcceptableContentType>]
                #(accessModifier) init(accept: [OpenAPIRuntime.AcceptHeaderContentType<Operations.#(uppercaseFirst(operation.operationId)).AcceptableContentType>] = .defaultValues()) {
                    self.accept = accept
                }
            }
            #(accessModifier) var headers: Operations.#(uppercaseFirst(operation.operationId)).Input.Headers
            #endif
            #if(operation.hasPath):
            /// - Remark: Generated from `#/paths#(operation.path)/#(lowercased(operation.method))/path`.
            #(accessModifier) struct Path: Sendable, Hashable {
                // Path parameters would be generated here
                #(accessModifier) init() {}
            }
            #(accessModifier) var path: Operations.#(uppercaseFirst(operation.operationId)).Input.Path
            #endif
            #if(operation.hasBody):
            #(accessModifier) var body: Operations.#(uppercaseFirst(operation.operationId)).Input.Body?
            #endif
            
            /// Creates a new `Input`.
            #(accessModifier) init(
                #if(operation.hasPath):
                path: Operations.#(uppercaseFirst(operation.operationId)).Input.Path,
                #endif
                #if(operation.hasQuery):
                query: Operations.#(uppercaseFirst(operation.operationId)).Input.Query = .init(),
                #endif
                #if(operation.hasHeaders):
                headers: Operations.#(uppercaseFirst(operation.operationId)).Input.Headers = .init(),
                #endif
                #if(operation.hasBody):
                body: Operations.#(uppercaseFirst(operation.operationId)).Input.Body? = nil
                #endif
            ) {
                #if(operation.hasPath):
                self.path = path
                #endif
                #if(operation.hasQuery):
                self.query = query
                #endif
                #if(operation.hasHeaders):
                self.headers = headers
                #endif
                #if(operation.hasBody):
                self.body = body
                #endif
            }
        }
        @frozen #(accessModifier) enum Output: Sendable, Hashable {
            #for(responseCode in operation.responseCodes):
            #if(responseCode == "200"):
            /// Success response.
            /// 
            /// - Remark: Generated from `#/paths#(operation.path)/#(lowercased(operation.method))/responses/200`.
            case ok(Operations.#(uppercaseFirst(operation.operationId)).Output.Ok)
            #endif
            #if(responseCode == "201"):
            /// Created response.
            ///
            /// - Remark: Generated from `#/paths#(operation.path)/#(lowercased(operation.method))/responses/201`.
            case created(Operations.#(uppercaseFirst(operation.operationId)).Output.Created)
            #endif
            #endfor
            /// Undocumented response.
            case undocumented(statusCode: Swift.Int, OpenAPIRuntime.UndocumentedPayload)
        }
    }
    #endfor
}